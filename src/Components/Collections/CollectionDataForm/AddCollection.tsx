import React from "react";
import CollectionDataForm from "./CollectionDataForm";

const AddCollectionContainer = () => {
  return <CollectionDataForm action="Add"/>;
};

export default AddCollectionContainer;
